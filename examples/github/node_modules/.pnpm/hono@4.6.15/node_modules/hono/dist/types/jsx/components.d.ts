import type { HtmlEscapedString } from ".pnpm/hono@4.6.15/node_modules/hono/utils/html";
import type {
	Child,
	FC,
	PropsWithChildren,
} from ".pnpm/hono@4.6.15/node_modules/hono/jsx";
export declare const childrenToString: (
	children: Child[],
) => Promise<HtmlEscapedString[]>;
export type ErrorHandler = (error: Error) => void;
export type FallbackRender = (error: Error) => Child;
/**
 * @experimental
 * `ErrorBoundary` is an experimental feature.
 * The API might be changed.
 */
export declare const ErrorBoundary: FC<
	PropsWithChildren<{
		fallback?: Child;
		fallbackRender?: FallbackRender;
		onError?: ErrorHandler;
	}>
>;
