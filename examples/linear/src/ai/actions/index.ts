import type { AntispaceMetadata } from "@antispace/sdk"

export const setNickname = async (_nickname: string, _anti: AntispaceMetadata) => {
  // placeholder
}

export const sayNickname = async (_anti: AntispaceMetadata) => {
  // placeholder
}
