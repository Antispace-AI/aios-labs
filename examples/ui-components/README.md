# Antispace UI Components Demo

UI component showcase for Antispace Apps SDK

This app is not built using Antispace CLI, instead it's written as a Hono.js standalone application.

```
npm install
npm run dev
```
